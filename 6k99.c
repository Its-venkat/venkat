#include<stdio.h>
int main()
float value;
int choice;
printf("1 . meter to feet\n");
printf("2 . kilometer to miles\n");
printf("enter yout choice(1 or 2)");
if(choice==1)
{
	scanf("%d\n",&choice);
	printf("value:\n");
	value in meters;
	scanf("%f\n"&values);
	float feet, value 3.2808 m;
	printf("%d meter to feet is %d\n",value,feet);
	{
		elseif(choice==2)
		{
			printf("value\n");||value||kilometers;
			scanf("%f\n",&value);
			float miles=value*0.621371;
			printf("%f kilometer to miles is %f\n",value,miles);
		}
		else
		{
			printf("invalid choice\n");
		}
		return 0;
	}
}
